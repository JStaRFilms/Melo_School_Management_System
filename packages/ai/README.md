# @school/ai

Shared AI SDK helpers for Lesson Knowledge Hub document generation.

## Usage

Import the centralized provider/model config, typed generation contracts, prompt builders, and retry helpers from `@school/ai` in route handlers or server utilities.

This package is document-generation oriented and intentionally does not add chat-first abstractions.
