{
  "name": "@school/convex",
  "version": "0.1.0",
  "private": true,
  "main": "./schema.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint . --ext .ts",
    "convex:dev": "pnpm -C ../.. convex:dev",
    "convex:deploy": "pnpm -C ../.. convex:deploy",
    "convex:codegen": "pnpm -C ../.. convex:codegen"
  },
  "dependencies": {
    "@convex-dev/better-auth": "^0.11.3",
    "@school/shared": "workspace:*",
    "better-auth": "^1.2.0",
    "convex": "^1.0.0",
    "pdfjs-dist": "^5.6.205"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.0.0"
  }
}
