import { mutation, query, type MutationCtx, type QueryCtx } from "../../_generated/server";
import type { Doc, Id } from "../../_generated/dataModel";
import { ConvexError, v } from "convex/values";
import { hasSchoolCapabilityV1, resolveSchoolMembershipV1 } from "../foundation/auth";
import { admissionFieldKinds, assertClosedConditionalGrammar, assertClosedValidationGrammar, audit, digest } from "./helpers";

const OPERATION_KEY_REUSED = "OPERATION_KEY_REUSED";
type Capability = "admissions.catalogue.manage" | "admissions.publish" | "admissions.sensitive.configure";
async function requireCatalogue(ctx: QueryCtx | MutationCtx, schoolId: Id<"schools">, capability: Capability) {
  const membership = await resolveSchoolMembershipV1(ctx, schoolId);
  if (!membership || !await hasSchoolCapabilityV1(ctx, membership, capability)) throw new ConvexError("Not found or access denied");
  return membership;
}
function slug(value: string) { const result = value.trim().toLowerCase(); if (!/^[a-z0-9]+(?:[_-][a-z0-9]+)*$/.test(result)) throw new ConvexError("Invalid slug"); return result; }
function text(value: string, label: string, max = 4_000) { const result = value.trim(); if (!result || result.length > max) throw new ConvexError(`${label} is required`); return result; }
function commandKey(value: string) { const key = value.trim(); if (!key || key.length > 128) throw new ConvexError("Invalid operation key"); return key; }
function canonical(value: unknown): string { if (Array.isArray(value)) return `[${value.map(canonical).join(",")}]`; if (value && typeof value === "object") return `{${Object.keys(value).sort().map((key) => `${JSON.stringify(key)}:${canonical((value as Record<string, unknown>)[key])}`).join(",")}}`; return JSON.stringify(typeof value === "string" ? value.trim() : value); }
function normalizeJson(value: string) { try { return canonical(JSON.parse(value)); } catch { return value.trim(); } }

const dataClassValidator = v.union(v.literal("public"), v.literal("internal"), v.literal("personal"), v.literal("child_confidential"), v.literal("highly_sensitive"), v.literal("financial_security"));
const requiredModeValidator = v.union(v.literal("required"), v.literal("optional"), v.literal("conditional"));
const fieldValidator = v.object({ fieldKey: v.string(), sectionKey: v.string(), kind: v.union(v.literal("text"), v.literal("textarea"), v.literal("select"), v.literal("date"), v.literal("number"), v.literal("boolean"), v.literal("checkbox"), v.literal("multi_select")), label: v.string(), requiredMode: requiredModeValidator, dataClass: dataClassValidator, purpose: v.optional(v.string()), retentionPolicyKey: v.optional(v.string()), audience: v.optional(v.string()), approvalEvidenceId: v.optional(v.id("schoolApprovalEvidence")), validationJson: v.string(), conditionalRuleJson: v.optional(v.string()), order: v.number() });
const requirementValidator = v.object({ requirementKey: v.string(), category: v.string(), label: v.string(), requiredMode: requiredModeValidator, acceptedMimeTypes: v.array(v.string()), maxBytes: v.number(), maxFiles: v.number(), sensitivity: dataClassValidator, purpose: v.string(), retentionPolicyKey: v.optional(v.string()), audience: v.optional(v.string()), approvalEvidenceId: v.optional(v.id("schoolApprovalEvidence")), conditionJson: v.optional(v.string()), order: v.number() });
const declarationValidator = v.object({ title: v.string(), body: v.string(), purpose: v.string() });
const createConfigurationValidator = v.object({ programme: v.object({ slug: v.string(), name: v.string(), description: v.optional(v.string()) }), intake: v.object({ slug: v.string(), name: v.string(), cycleLabel: v.string(), opensAt: v.number(), closesAt: v.number(), targetClassId: v.optional(v.id("classes")) }), product: v.object({ slug: v.string(), name: v.string() }), declaration: declarationValidator, fields: v.array(fieldValidator), requirements: v.array(requirementValidator) });
const replaceConfigurationValidator = v.object({ intake: v.object({ name: v.string(), cycleLabel: v.string(), opensAt: v.number(), closesAt: v.number(), targetClassId: v.optional(v.id("classes")), description: v.optional(v.string()) }), declaration: declarationValidator, fields: v.array(fieldValidator), requirements: v.array(requirementValidator), price: v.optional(v.object({ amountMinor: v.number(), currency: v.string(), refundPolicyKey: v.string(), feeDisclosure: v.string(), approvalEvidenceId: v.optional(v.id("schoolApprovalEvidence")) })) });
type Field = typeof fieldValidator.type;
type Requirement = typeof requirementValidator.type;
type Declaration = typeof declarationValidator.type;
type CreateConfiguration = typeof createConfigurationValidator.type;
type ReplaceConfiguration = typeof replaceConfigurationValidator.type;
type Result = { programmeId: Id<"admissionsProgrammes">; intakeId: Id<"admissionsIntakes">; productId: Id<"admissionsProducts">; formVersionId: Id<"admissionsFormVersions">; declarationVersionId: Id<"admissionsDeclarationVersions">; priceId: Id<"admissionsProductPrices"> | null; status: "draft" | "published"; replayed: boolean };
const resultValidator = v.object({ programmeId: v.id("admissionsProgrammes"), intakeId: v.id("admissionsIntakes"), productId: v.id("admissionsProducts"), formVersionId: v.id("admissionsFormVersions"), declarationVersionId: v.id("admissionsDeclarationVersions"), priceId: v.union(v.id("admissionsProductPrices"), v.null()), status: v.union(v.literal("draft"), v.literal("published")), replayed: v.boolean() });

async function assertEvidence(ctx: MutationCtx, schoolId: Id<"schools">, evidenceId: Id<"schoolApprovalEvidence">, approvalClass: "privacy" | "finance", subject: { type: string; key: string }) {
  const evidence = await ctx.db.get(evidenceId); const approver = evidence?.approvedByUserId ? await ctx.db.get(evidence.approvedByUserId) : null;
  if (!evidence || evidence.schoolId !== schoolId || evidence.approvalClass !== approvalClass || evidence.subjectType !== subject.type || evidence.subjectKey !== subject.key || !approver || approver.schoolId !== schoolId || approver.isArchived || evidence.revokedAt || evidence.approvedAt > Date.now() || (evidence.expiresAt && evidence.expiresAt <= Date.now())) throw new ConvexError(`${approvalClass === "finance" ? "Finance" : "Privacy"} approval is unavailable`);
}
async function assertFieldEvidence(ctx: MutationCtx, schoolId: Id<"schools">, item: Field | Requirement, dataClass: string, subject: { type: string; key: string }) {
  if (item.approvalEvidenceId) await assertEvidence(ctx, schoolId, item.approvalEvidenceId, "privacy", subject);
  if (dataClass !== "highly_sensitive" && dataClass !== "financial_security") return;
  await requireCatalogue(ctx, schoolId, "admissions.sensitive.configure");
  if (!item.approvalEvidenceId || !item.purpose?.trim() || !item.retentionPolicyKey?.trim() || !item.audience?.trim()) throw new ConvexError("Sensitive configuration needs purpose, audience, retention, and approval");
}
// Legacy single-row commands retain their existing governance contract.
async function assertSensitiveConfiguration(ctx: MutationCtx, schoolId: Id<"schools">, dataClass: string, approvalEvidenceId?: Id<"schoolApprovalEvidence">, purpose?: string, retentionPolicyKey?: string, audience?: string, subject?: { type: string; key: string }) {
  if (dataClass !== "highly_sensitive" && dataClass !== "financial_security") return;
  await requireCatalogue(ctx, schoolId, "admissions.sensitive.configure");
  if (!approvalEvidenceId || !purpose?.trim() || !retentionPolicyKey?.trim() || !audience?.trim()) throw new ConvexError("Sensitive configuration needs purpose, audience, retention, and approval");
  const evidence = await ctx.db.get(approvalEvidenceId); const approver = evidence?.approvedByUserId ? await ctx.db.get(evidence.approvedByUserId) : null;
  if (!evidence || evidence.schoolId !== schoolId || evidence.approvalClass !== "privacy" || !approver || approver.schoolId !== schoolId || approver.isArchived || evidence.revokedAt || evidence.approvedAt > Date.now() || (evidence.expiresAt && evidence.expiresAt <= Date.now()) || (subject && (evidence.subjectType !== subject.type || evidence.subjectKey !== subject.key))) throw new ConvexError("Sensitive configuration approval is unavailable");
}
async function assertConfiguration(ctx: MutationCtx, schoolId: Id<"schools">, configuration: { intake: { opensAt: number; closesAt: number; targetClassId?: Id<"classes"> }; declaration: Declaration; fields: Field[]; requirements: Requirement[] }) {
  if (!Number.isFinite(configuration.intake.opensAt) || !Number.isFinite(configuration.intake.closesAt) || configuration.intake.opensAt >= configuration.intake.closesAt) throw new ConvexError("Invalid intake");
  text(configuration.declaration.title, "Declaration title", 256); text(configuration.declaration.body, "Declaration body", 20_000); text(configuration.declaration.purpose, "Declaration purpose", 1_000);
  if (configuration.fields.length > 200 || configuration.requirements.length > 100) throw new ConvexError("Campaign configuration is too large");
  if (configuration.intake.targetClassId) { const row = await ctx.db.get(configuration.intake.targetClassId); if (!row || row.schoolId !== schoolId || row.isArchived) throw new ConvexError("Invalid intake"); }
  const fields = new Map<string, Field>(); const fieldOrders = new Set<number>();
  for (const field of configuration.fields) { const key = slug(field.fieldKey); if (fields.has(key) || fieldOrders.has(field.order) || !Number.isInteger(field.order) || field.order < 0 || !(admissionFieldKinds as readonly string[]).includes(field.kind)) throw new ConvexError("Invalid field"); fields.set(key, field); fieldOrders.add(field.order); text(field.sectionKey, "Section", 128); text(field.label, "Field label", 256); assertClosedValidationGrammar(field.validationJson); }
  for (const [key, field] of fields) { if (field.requiredMode === "conditional" && !field.conditionalRuleJson) throw new ConvexError("Conditional rule is required"); const rule = field.conditionalRuleJson ? assertClosedConditionalGrammar(field.conditionalRuleJson) : null; const controller = rule ? fields.get(slug(rule.fieldKey)) : null; if (rule && (!controller || controller.order >= field.order)) throw new ConvexError("Conditional controller must exist before its field"); await assertFieldEvidence(ctx, schoolId, field, field.dataClass, { type: "admissions_field", key }); }
  const requirementKeys = new Set<string>(); const requirementOrders = new Set<number>();
  for (const requirement of configuration.requirements) { const key = slug(requirement.requirementKey); if (requirementKeys.has(key) || requirementOrders.has(requirement.order) || !Number.isInteger(requirement.order) || requirement.order < 0 || !Number.isInteger(requirement.maxBytes) || requirement.maxBytes < 1 || requirement.maxBytes > 50_000_000 || !Number.isInteger(requirement.maxFiles) || requirement.maxFiles < 1 || requirement.maxFiles > 10 || requirement.acceptedMimeTypes.length < 1 || requirement.acceptedMimeTypes.length > 20 || requirement.acceptedMimeTypes.some((mime) => !/^[\w.+-]+\/[\w.+-]+$/.test(mime))) throw new ConvexError("Invalid document requirement"); requirementKeys.add(key); requirementOrders.add(requirement.order); text(requirement.category, "Document category", 128); text(requirement.label, "Document label", 256); text(requirement.purpose, "Document purpose", 1_000); if (requirement.requiredMode === "conditional" && !requirement.conditionJson) throw new ConvexError("Conditional rule is required"); const rule = requirement.conditionJson ? assertClosedConditionalGrammar(requirement.conditionJson) : null; const controller = rule ? fields.get(slug(rule.fieldKey)) : null; if (rule && (!controller || controller.order >= requirement.order)) throw new ConvexError("Conditional controller must exist before its requirement"); await assertFieldEvidence(ctx, schoolId, requirement, requirement.sensitivity, { type: "admissions_document_requirement", key }); }
}
function semanticDigest(command: "create" | "replace", targetStatus: "draft" | "published", configuration: CreateConfiguration | ReplaceConfiguration, intakeId?: Id<"admissionsIntakes">) {
  const normalized = { command, targetStatus, intakeId: intakeId ? String(intakeId) : null, configuration: { ...configuration, fields: [...configuration.fields].map((item) => ({ ...item, fieldKey: item.fieldKey.trim().toLowerCase(), validationJson: normalizeJson(item.validationJson), conditionalRuleJson: item.conditionalRuleJson ? normalizeJson(item.conditionalRuleJson) : undefined })).sort((a, b) => a.order - b.order || a.fieldKey.localeCompare(b.fieldKey)), requirements: [...configuration.requirements].map((item) => ({ ...item, requirementKey: item.requirementKey.trim().toLowerCase(), conditionJson: item.conditionJson ? normalizeJson(item.conditionJson) : undefined })).sort((a, b) => a.order - b.order || a.requirementKey.localeCompare(b.requirementKey)) } };
  return digest(canonical(normalized));
}
async function replay(ctx: MutationCtx, schoolId: Id<"schools">, actorUserId: Id<"users">, command: "create" | "replace", key: string, requestDigest: string): Promise<Result | null> {
  const row = await ctx.db.query("admissionsCampaignOperations").withIndex("by_school_and_actor_user_and_command_and_operation_key", (q) => q.eq("schoolId", schoolId).eq("actorUserId", actorUserId).eq("command", command).eq("operationKey", key)).unique();
  if (!row) return null; if (row.requestDigest !== requestDigest) throw new ConvexError(OPERATION_KEY_REUSED);
  return { programmeId: row.programmeId, intakeId: row.intakeId, productId: row.productId, formVersionId: row.formVersionId, declarationVersionId: row.declarationVersionId, priceId: row.priceId ?? null, status: row.status, replayed: true };
}
async function insertGraph(ctx: MutationCtx, args: { schoolId: Id<"schools">; actorUserId: Id<"users">; programmeId: Id<"admissionsProgrammes">; intakeId: Id<"admissionsIntakes">; declaration: Declaration; fields: Field[]; requirements: Requirement[]; declarationVersion: number; formVersion: number; targetStatus: "draft" | "published"; existingDeclarationId?: Id<"admissionsDeclarationVersions"> }) {
  const now = Date.now(); const published = args.targetStatus === "published";
  const declarationVersionId = args.existingDeclarationId ?? await ctx.db.insert("admissionsDeclarationVersions", { schoolId: args.schoolId, programmeId: args.programmeId, version: args.declarationVersion, title: text(args.declaration.title, "Declaration title", 256), body: text(args.declaration.body, "Declaration body", 20_000), bodyDigest: await digest(text(args.declaration.body, "Declaration body", 20_000)), purpose: text(args.declaration.purpose, "Declaration purpose", 1_000), status: published ? "published" : "draft", ...(published ? { publishedAt: now, publishedBy: args.actorUserId } : {}), createdAt: now, updatedAt: now });
  const formVersionId = await ctx.db.insert("admissionsFormVersions", { schoolId: args.schoolId, programmeId: args.programmeId, intakeId: args.intakeId, version: args.formVersion, schemaVersion: "1", legalNamePolicyVersion: 2, status: published ? "published" : "draft", ...(published ? { publishedAt: now, publishedBy: args.actorUserId } : {}), createdAt: now, updatedAt: now });
  for (const field of args.fields) await ctx.db.insert("admissionsFormFields", { schoolId: args.schoolId, formVersionId, fieldKey: slug(field.fieldKey), sectionKey: text(field.sectionKey, "Section", 128), kind: field.kind, label: text(field.label, "Field label", 256), requiredMode: field.requiredMode, dataClass: field.dataClass, ...(field.purpose?.trim() ? { purpose: field.purpose.trim() } : {}), ...(field.retentionPolicyKey?.trim() ? { retentionPolicyKey: field.retentionPolicyKey.trim() } : {}), ...(field.audience?.trim() ? { audience: field.audience.trim() } : {}), ...(field.approvalEvidenceId ? { approvalEvidenceId: field.approvalEvidenceId } : {}), validationJson: field.validationJson, ...(field.conditionalRuleJson ? { conditionalRuleJson: field.conditionalRuleJson } : {}), order: field.order, status: "active", createdAt: now, updatedAt: now });
  for (const requirement of args.requirements) await ctx.db.insert("admissionsDocumentRequirements", { schoolId: args.schoolId, formVersionId, requirementKey: slug(requirement.requirementKey), category: text(requirement.category, "Document category", 128), label: text(requirement.label, "Document label", 256), requiredMode: requirement.requiredMode, acceptedMimeTypes: requirement.acceptedMimeTypes, maxBytes: requirement.maxBytes, maxFiles: requirement.maxFiles, sensitivity: requirement.sensitivity, purpose: text(requirement.purpose, "Document purpose", 1_000), ...(requirement.retentionPolicyKey?.trim() ? { retentionPolicyKey: requirement.retentionPolicyKey.trim() } : {}), ...(requirement.audience?.trim() ? { audience: requirement.audience.trim() } : {}), ...(requirement.approvalEvidenceId ? { approvalEvidenceId: requirement.approvalEvidenceId } : {}), ...(requirement.conditionJson ? { conditionJson: requirement.conditionJson } : {}), order: requirement.order, createdAt: now, updatedAt: now });
  return { declarationVersionId, formVersionId, now };
}

export const createCampaignConfiguration = mutation({
  args: { schoolId: v.id("schools"), operationKey: v.string(), targetStatus: v.union(v.literal("draft"), v.literal("published")), configuration: createConfigurationValidator }, returns: resultValidator,
  handler: async (ctx, args): Promise<Result> => { const member = await requireCatalogue(ctx, args.schoolId, "admissions.catalogue.manage"); if (args.targetStatus === "published") await requireCatalogue(ctx, args.schoolId, "admissions.publish"); const key = commandKey(args.operationKey); const requestDigest = await semanticDigest("create", args.targetStatus, args.configuration); const prior = await replay(ctx, args.schoolId, member.userId, "create", key, requestDigest); if (prior) return prior; slug(args.configuration.programme.slug); slug(args.configuration.intake.slug); slug(args.configuration.product.slug); text(args.configuration.programme.name, "Programme name", 256); text(args.configuration.intake.name, "Intake name", 256); text(args.configuration.intake.cycleLabel, "Cycle label", 128); text(args.configuration.product.name, "Product name", 256); await assertConfiguration(ctx, args.schoolId, args.configuration); const duplicates = await Promise.all([ctx.db.query("admissionsProgrammes").withIndex("by_school_and_slug", (q) => q.eq("schoolId", args.schoolId).eq("slug", slug(args.configuration.programme.slug))).unique(), ctx.db.query("admissionsIntakes").withIndex("by_school_and_slug", (q) => q.eq("schoolId", args.schoolId).eq("slug", slug(args.configuration.intake.slug))).unique(), ctx.db.query("admissionsProducts").withIndex("by_school_and_slug", (q) => q.eq("schoolId", args.schoolId).eq("slug", slug(args.configuration.product.slug))).unique()]); if (duplicates.some(Boolean)) throw new ConvexError("Campaign slug already exists"); const now = Date.now(); const published = args.targetStatus === "published"; const programmeId = await ctx.db.insert("admissionsProgrammes", { schoolId: args.schoolId, slug: slug(args.configuration.programme.slug), name: text(args.configuration.programme.name, "Programme name", 256), ...(args.configuration.programme.description?.trim() ? { description: args.configuration.programme.description.trim() } : {}), status: published ? "published" : "draft", createdAt: now, updatedAt: now }); const intakeId = await ctx.db.insert("admissionsIntakes", { schoolId: args.schoolId, programmeId, slug: slug(args.configuration.intake.slug), name: text(args.configuration.intake.name, "Intake name", 256), cycleLabel: text(args.configuration.intake.cycleLabel, "Cycle label", 128), ...(args.configuration.intake.targetClassId ? { targetClassId: args.configuration.intake.targetClassId } : {}), opensAt: args.configuration.intake.opensAt, closesAt: args.configuration.intake.closesAt, status: published ? "open" : "draft", createdAt: now, updatedAt: now }); const productId = await ctx.db.insert("admissionsProducts", { schoolId: args.schoolId, intakeId, slug: slug(args.configuration.product.slug), name: text(args.configuration.product.name, "Product name", 256), slotCount: 1, status: published ? "active" : "draft", createdAt: now, updatedAt: now }); const graph = await insertGraph(ctx, { schoolId: args.schoolId, actorUserId: member.userId, programmeId, intakeId, declaration: args.configuration.declaration, fields: args.configuration.fields, requirements: args.configuration.requirements, declarationVersion: 1, formVersion: 1, targetStatus: args.targetStatus }); await ctx.db.insert("admissionsCampaignOperations", { schoolId: args.schoolId, actorUserId: member.userId, command: "create", operationKey: key, requestDigest, programmeId, intakeId, productId, formVersionId: graph.formVersionId, declarationVersionId: graph.declarationVersionId, status: args.targetStatus, createdAt: now }); await audit({ ctx, schoolId: args.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.campaign_created_atomically", entityType: "intake", entityId: String(intakeId), outcome: "success" }); return { programmeId, intakeId, productId, formVersionId: graph.formVersionId, declarationVersionId: graph.declarationVersionId, priceId: null, status: args.targetStatus, replayed: false }; },
});

export const replaceCampaignConfiguration = mutation({
  args: { schoolId: v.id("schools"), intakeId: v.id("admissionsIntakes"), operationKey: v.string(), targetStatus: v.union(v.literal("draft"), v.literal("published")), configuration: replaceConfigurationValidator }, returns: resultValidator,
  handler: async (ctx, args): Promise<Result> => { const member = await requireCatalogue(ctx, args.schoolId, "admissions.catalogue.manage"); if (args.targetStatus === "published") await requireCatalogue(ctx, args.schoolId, "admissions.publish"); const key = commandKey(args.operationKey); const requestDigest = await semanticDigest("replace", args.targetStatus, args.configuration, args.intakeId); const prior = await replay(ctx, args.schoolId, member.userId, "replace", key, requestDigest); if (prior) return prior; const intake = await ctx.db.get(args.intakeId); if (!intake || intake.schoolId !== args.schoolId || intake.status === "archived") throw new ConvexError("Invalid intake"); const programme = await ctx.db.get(intake.programmeId); if (!programme || programme.schoolId !== args.schoolId) throw new ConvexError("Invalid intake"); await assertConfiguration(ctx, args.schoolId, args.configuration); text(args.configuration.intake.name, "Intake name", 256); text(args.configuration.intake.cycleLabel, "Cycle label", 128); if (args.configuration.intake.description !== undefined) text(args.configuration.intake.description, "Programme description", 4_000); const products = await ctx.db.query("admissionsProducts").withIndex("by_school_and_intake", (q) => q.eq("schoolId", args.schoolId).eq("intakeId", intake._id)).take(2); const forms = (await ctx.db.query("admissionsFormVersions").withIndex("by_school_and_programme", (q) => q.eq("schoolId", args.schoolId).eq("programmeId", programme._id)).take(101)).filter((item) => item.intakeId === intake._id); const declarations = await ctx.db.query("admissionsDeclarationVersions").withIndex("by_school_and_programme_and_version", (q) => q.eq("schoolId", args.schoolId).eq("programmeId", programme._id).gte("version", 0)).take(101); const operations = await ctx.db.query("admissionsCampaignOperations").withIndex("by_intake", (q) => q.eq("intakeId", intake._id)).take(1); if (products.length !== 1 || forms.length > 100 || declarations.length > 100) throw new ConvexError("RECOVERY_GRAPH_AMBIGUOUS"); if (!operations.length && (intake.status !== "draft" || forms.filter((item) => item.status === "draft").length !== 1 || declarations.filter((item) => item.status === "draft").length !== 1)) throw new ConvexError("RECOVERY_GRAPH_AMBIGUOUS"); const product = products[0]; const publishedForms = forms.filter((item) => item.status === "published"); const publishedDeclarations = declarations.filter((item) => item.status === "published"); if (publishedForms.length > 1 || publishedDeclarations.length > 1) throw new ConvexError("Campaign publication evidence is inconsistent"); const activePrices = await ctx.db.query("admissionsProductPrices").withIndex("by_product_and_version", (q) => q.eq("productId", product._id)).take(101); if (activePrices.length > 100) throw new ConvexError("Campaign has too many prices"); const currentPrice = activePrices.filter((item) => item.status === "published").sort((a, b) => b.version - a.version)[0]; const requestedPrice = args.configuration.price; const changedPrice = !!requestedPrice && (!currentPrice || currentPrice.amountMinor !== requestedPrice.amountMinor || currentPrice.currency !== requestedPrice.currency.trim().toUpperCase() || currentPrice.refundPolicyKey !== requestedPrice.refundPolicyKey.trim() || currentPrice.feeDisclosure !== requestedPrice.feeDisclosure.trim()); if (changedPrice && args.targetStatus !== "published") throw new ConvexError("Price changes require publication"); let nextPrice: { version: number; amountMinor: number; currency: string; refundPolicyKey: string; feeDisclosure: string; approvalEvidenceId: Id<"schoolApprovalEvidence"> } | null = null; if (changedPrice) { if (!requestedPrice || !Number.isInteger(requestedPrice.amountMinor) || requestedPrice.amountMinor < 0 || !requestedPrice.approvalEvidenceId) throw new ConvexError("Finance approval is required"); const version = Math.max(0, ...activePrices.map((item) => item.version)) + 1; const currency = text(requestedPrice.currency, "Currency", 8).toUpperCase(); const refundPolicyKey = text(requestedPrice.refundPolicyKey, "Refund policy", 128); const feeDisclosure = text(requestedPrice.feeDisclosure, "Fee disclosure", 4_000); await assertEvidence(ctx, args.schoolId, requestedPrice.approvalEvidenceId, "finance", { type: "admissions_price", key: `${String(product._id)}:${version}` }); nextPrice = { version, amountMinor: requestedPrice.amountMinor, currency, refundPolicyKey, feeDisclosure, approvalEvidenceId: requestedPrice.approvalEvidenceId }; }
    const declarationChanged = !publishedDeclarations[0] || publishedDeclarations[0].title !== args.configuration.declaration.title.trim() || publishedDeclarations[0].body !== args.configuration.declaration.body.trim() || publishedDeclarations[0].purpose !== args.configuration.declaration.purpose.trim(); if (args.targetStatus === "published" && declarationChanged) { const siblingLists = await Promise.all(["draft", "open", "paused", "closed"].map((status) => ctx.db.query("admissionsIntakes").withIndex("by_programme_and_status", (q) => q.eq("programmeId", programme._id).eq("status", status as "draft" | "open" | "paused" | "closed")).take(2))); if (siblingLists.flat().some((item) => item._id !== intake._id)) throw new ConvexError("DECLARATION_SCOPE_AMBIGUOUS"); }
    const now = Date.now(); const publish = args.targetStatus === "published"; if (publish) { await ctx.db.patch(programme._id, { name: programme.name, ...(args.configuration.intake.description !== undefined ? { description: args.configuration.intake.description.trim() } : {}), ...(programme.status === "draft" ? { status: "published" } : {}), updatedAt: now }); await ctx.db.patch(intake._id, { name: text(args.configuration.intake.name, "Intake name", 256), cycleLabel: text(args.configuration.intake.cycleLabel, "Cycle label", 128), ...(args.configuration.intake.targetClassId ? { targetClassId: args.configuration.intake.targetClassId } : {}), opensAt: args.configuration.intake.opensAt, closesAt: args.configuration.intake.closesAt, ...(intake.status === "draft" ? { status: "open" } : {}), updatedAt: now }); if (product.status === "draft") await ctx.db.patch(product._id, { status: "active", updatedAt: now }); for (const row of publishedForms) await ctx.db.patch(row._id, { status: "retired", updatedAt: now }); if (declarationChanged) for (const row of publishedDeclarations) await ctx.db.patch(row._id, { status: "retired", updatedAt: now }); }
    const graph = await insertGraph(ctx, { schoolId: args.schoolId, actorUserId: member.userId, programmeId: programme._id, intakeId: intake._id, declaration: args.configuration.declaration, fields: args.configuration.fields, requirements: args.configuration.requirements, declarationVersion: Math.max(0, ...declarations.map((item) => item.version)) + 1, formVersion: Math.max(0, ...forms.map((item) => item.version)) + 1, targetStatus: args.targetStatus, ...(publish && !declarationChanged && publishedDeclarations[0] ? { existingDeclarationId: publishedDeclarations[0]._id } : {}) }); let priceId: Id<"admissionsProductPrices"> | null = null; if (nextPrice) priceId = await ctx.db.insert("admissionsProductPrices", { schoolId: args.schoolId, productId: product._id, ...nextPrice, effectiveFrom: now, status: "published", createdAt: now, updatedAt: now }); await ctx.db.insert("admissionsCampaignOperations", { schoolId: args.schoolId, actorUserId: member.userId, command: "replace", operationKey: key, requestDigest, programmeId: programme._id, intakeId: intake._id, productId: product._id, formVersionId: graph.formVersionId, declarationVersionId: graph.declarationVersionId, ...(priceId ? { priceId } : {}), status: args.targetStatus, createdAt: now }); await audit({ ctx, schoolId: args.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.campaign_replaced_atomically", entityType: "intake", entityId: String(intake._id), outcome: "success" }); return { programmeId: programme._id, intakeId: intake._id, productId: product._id, formVersionId: graph.formVersionId, declarationVersionId: graph.declarationVersionId, priceId, status: args.targetStatus, replayed: false }; },
});

export const listLegacyCampaignRecovery = query({
  args: { schoolId: v.id("schools") }, returns: v.array(v.object({ programmeId: v.id("admissionsProgrammes"), programmeName: v.string(), intakeId: v.id("admissionsIntakes"), intakeName: v.string(), createdAt: v.number(), updatedAt: v.number(), productCount: v.number(), formCount: v.number(), declarationCount: v.number(), missingProduct: v.boolean(), missingForm: v.boolean(), missingDeclaration: v.boolean(), ambiguousProduct: v.boolean(), ambiguousForm: v.boolean(), ambiguousDeclaration: v.boolean(), recoveryState: v.literal("review_required") })),
  handler: async (ctx, args) => { await requireCatalogue(ctx, args.schoolId, "admissions.catalogue.manage"); const intakes = await ctx.db.query("admissionsIntakes").withIndex("by_school_and_status_and_opens_at", (q) => q.eq("schoolId", args.schoolId).eq("status", "draft")).take(50); const rows = await Promise.all(intakes.map(async (intake) => { const [operations, programme, products, forms, declarations] = await Promise.all([ctx.db.query("admissionsCampaignOperations").withIndex("by_intake", (q) => q.eq("intakeId", intake._id)).take(1), ctx.db.get(intake.programmeId), ctx.db.query("admissionsProducts").withIndex("by_school_and_intake", (q) => q.eq("schoolId", args.schoolId).eq("intakeId", intake._id)).take(3), ctx.db.query("admissionsFormVersions").withIndex("by_intake_and_status", (q) => q.eq("intakeId", intake._id).eq("status", "draft")).take(3), ctx.db.query("admissionsDeclarationVersions").withIndex("by_programme_and_status", (q) => q.eq("programmeId", intake.programmeId).eq("status", "draft")).take(3)]); if (operations.length || !programme) return null; return { programmeId: programme._id, programmeName: programme.name, intakeId: intake._id, intakeName: intake.name, createdAt: intake.createdAt, updatedAt: intake.updatedAt, productCount: products.length, formCount: forms.length, declarationCount: declarations.length, missingProduct: products.length === 0, missingForm: forms.length === 0, missingDeclaration: declarations.length === 0, ambiguousProduct: products.length > 1, ambiguousForm: forms.length > 1, ambiguousDeclaration: declarations.length > 1, recoveryState: "review_required" as const }; })); return rows.filter((row): row is NonNullable<typeof row> => row !== null); },
});

/** Tenant-scoped persisted catalogue projection for B3; no private application data. */
export const getCatalogue = query({
  args: { schoolId: v.id("schools") },
  returns: v.object({
    programmes: v.array(v.object({ id: v.id("admissionsProgrammes"), slug: v.string(), name: v.string(), description: v.union(v.string(), v.null()), status: v.string() })),
    intakes: v.array(v.object({ id: v.id("admissionsIntakes"), programmeId: v.id("admissionsProgrammes"), slug: v.string(), name: v.string(), cycleLabel: v.string(), status: v.string(), opensAt: v.number(), closesAt: v.number() })),
    products: v.array(v.object({ id: v.id("admissionsProducts"), intakeId: v.id("admissionsIntakes"), slug: v.string(), name: v.string(), status: v.string() })),
    forms: v.array(v.object({ id: v.id("admissionsFormVersions"), intakeId: v.union(v.id("admissionsIntakes"), v.null()), version: v.number(), schemaVersion: v.string(), status: v.string() })),
    declarations: v.array(v.object({ id: v.id("admissionsDeclarationVersions"), programmeId: v.id("admissionsProgrammes"), version: v.number(), title: v.string(), body: v.string(), status: v.string() })),
  }),
  handler: async (ctx, args) => {
    await requireCatalogue(ctx, args.schoolId, "admissions.catalogue.manage");
    const programmes: Doc<"admissionsProgrammes">[] = await ctx.db.query("admissionsProgrammes").withIndex("by_school", (q) => q.eq("schoolId", args.schoolId)).take(100);
    const [intakes, forms, declarationLists]: [Doc<"admissionsIntakes">[], Doc<"admissionsFormVersions">[], Doc<"admissionsDeclarationVersions">[][]] = await Promise.all([
      ctx.db.query("admissionsIntakes").withIndex("by_school", (q) => q.eq("schoolId", args.schoolId)).take(200),
      ctx.db.query("admissionsFormVersions").withIndex("by_school_and_programme", (q) => q.eq("schoolId", args.schoolId)).take(200),
      Promise.all(programmes.map((programme) => ctx.db.query("admissionsDeclarationVersions").withIndex("by_programme_and_status", (q) => q.eq("programmeId", programme._id)).take(100))),
    ]);
    const productLists = await Promise.all(intakes.map((intake) => ctx.db.query("admissionsProducts").withIndex("by_school_and_intake", (q) => q.eq("schoolId", args.schoolId).eq("intakeId", intake._id)).take(20)));
    const products = productLists.flat();
    const declarations = declarationLists.flat();
    return {
      programmes: programmes.map((item) => ({ id: item._id, slug: item.slug, name: item.name, description: item.description ?? null, status: item.status })),
      intakes: intakes.map((item) => ({ id: item._id, programmeId: item.programmeId, slug: item.slug, name: item.name, cycleLabel: item.cycleLabel, status: item.status, opensAt: item.opensAt, closesAt: item.closesAt })),
      products: products.map((item) => ({ id: item._id, intakeId: item.intakeId, slug: item.slug, name: item.name, status: item.status })),
      forms: forms.map((item) => ({ id: item._id, intakeId: item.intakeId ?? null, version: item.version, schemaVersion: item.schemaVersion, status: item.status })),
      declarations: declarations.map((item) => ({ id: item._id, programmeId: item.programmeId, version: item.version, title: item.title, body: item.body, status: item.status })),
    };
  },
});

/** Read-only catalogue projection for publication review. Draft editing still requires catalogue.manage. */
export const getPublicationReview = query({
  args: { schoolId: v.id("schools") },
  returns: v.object({ programmes: v.array(v.object({ id: v.id("admissionsProgrammes"), slug: v.string(), name: v.string(), status: v.string() })), intakes: v.array(v.object({ id: v.id("admissionsIntakes"), programmeId: v.id("admissionsProgrammes"), slug: v.string(), name: v.string(), status: v.string(), opensAt: v.number(), closesAt: v.number() })), products: v.array(v.object({ id: v.id("admissionsProducts"), intakeId: v.id("admissionsIntakes"), slug: v.string(), name: v.string(), status: v.string() })), forms: v.array(v.object({ id: v.id("admissionsFormVersions"), intakeId: v.union(v.id("admissionsIntakes"), v.null()), version: v.number(), schemaVersion: v.string(), status: v.string() })) }),
  handler: async (ctx, args) => {
    await requireCatalogue(ctx, args.schoolId, "admissions.publish");
    const programmes = await ctx.db.query("admissionsProgrammes").withIndex("by_school", (q) => q.eq("schoolId", args.schoolId)).take(100);
    const [intakes, forms] = await Promise.all([
      ctx.db.query("admissionsIntakes").withIndex("by_school", (q) => q.eq("schoolId", args.schoolId)).take(200),
      ctx.db.query("admissionsFormVersions").withIndex("by_school_and_programme", (q) => q.eq("schoolId", args.schoolId)).take(200),
    ]);
    const productLists = await Promise.all(intakes.map((intake) => ctx.db.query("admissionsProducts").withIndex("by_school_and_intake", (q) => q.eq("schoolId", args.schoolId).eq("intakeId", intake._id)).take(20)));
    const products = productLists.flat();
    return { programmes: programmes.map((item) => ({ id: item._id, slug: item.slug, name: item.name, status: item.status })), intakes: intakes.map((item) => ({ id: item._id, programmeId: item.programmeId, slug: item.slug, name: item.name, status: item.status, opensAt: item.opensAt, closesAt: item.closesAt })), products: products.map((item) => ({ id: item._id, intakeId: item.intakeId, slug: item.slug, name: item.name, status: item.status })), forms: forms.map((item) => ({ id: item._id, intakeId: item.intakeId ?? null, version: item.version, schemaVersion: item.schemaVersion, status: item.status })) };
  },
});

export const getFormConfiguration = query({
  args: { formVersionId: v.id("admissionsFormVersions") },
  returns: v.object({ form: v.object({ id: v.id("admissionsFormVersions"), version: v.number(), legalNamePolicyVersion: v.number(), status: v.string() }), fields: v.array(v.object({ id: v.id("admissionsFormFields"), key: v.string(), sectionKey: v.string(), label: v.string(), kind: v.string(), requiredMode: v.string(), dataClass: v.string(), purpose: v.union(v.string(), v.null()), retentionPolicyKey: v.union(v.string(), v.null()), audience: v.union(v.string(), v.null()), approvalEvidenceId: v.union(v.id("schoolApprovalEvidence"), v.null()), validationJson: v.string(), conditionalRuleJson: v.union(v.string(), v.null()), order: v.number() })), requirements: v.array(v.object({ id: v.id("admissionsDocumentRequirements"), key: v.string(), label: v.string(), category: v.string(), requiredMode: v.string(), sensitivity: v.string(), acceptedMimeTypes: v.array(v.string()), maxBytes: v.number(), maxFiles: v.number(), purpose: v.string(), retentionPolicyKey: v.union(v.string(), v.null()), audience: v.union(v.string(), v.null()), approvalEvidenceId: v.union(v.id("schoolApprovalEvidence"), v.null()), conditionJson: v.union(v.string(), v.null()), order: v.number() })) }),
  handler: async (ctx, args) => {
    const form = await ctx.db.get(args.formVersionId); if (!form) throw new ConvexError("Not found or access denied");
    await requireCatalogue(ctx, form.schoolId, "admissions.catalogue.manage");
    const [fields, requirements] = await Promise.all([ctx.db.query("admissionsFormFields").withIndex("by_form_version_and_order", (q) => q.eq("formVersionId", form._id)).take(200), ctx.db.query("admissionsDocumentRequirements").withIndex("by_form_version_and_order", (q) => q.eq("formVersionId", form._id)).take(100)]);
    return { form: { id: form._id, version: form.version, legalNamePolicyVersion: form.legalNamePolicyVersion ?? 1, status: form.status }, fields: fields.map((field) => ({ id: field._id, key: field.fieldKey, sectionKey: field.sectionKey, label: field.label, kind: field.kind, requiredMode: field.requiredMode, dataClass: field.dataClass, purpose: field.purpose ?? null, retentionPolicyKey: field.retentionPolicyKey ?? null, audience: field.audience ?? null, approvalEvidenceId: field.approvalEvidenceId ?? null, validationJson: field.validationJson, conditionalRuleJson: field.conditionalRuleJson ?? null, order: field.order })), requirements: requirements.map((requirement) => ({ id: requirement._id, key: requirement.requirementKey, label: requirement.label, category: requirement.category, requiredMode: requirement.requiredMode, sensitivity: requirement.sensitivity, acceptedMimeTypes: requirement.acceptedMimeTypes, maxBytes: requirement.maxBytes, maxFiles: requirement.maxFiles, purpose: requirement.purpose, retentionPolicyKey: requirement.retentionPolicyKey ?? null, audience: requirement.audience ?? null, approvalEvidenceId: requirement.approvalEvidenceId ?? null, conditionJson: requirement.conditionJson ?? null, order: requirement.order })) };
  },
});

export const listApprovalEvidence = query({
  args: { schoolId: v.id("schools") },
  returns: v.array(v.object({ id: v.id("schoolApprovalEvidence"), approvalClass: v.string(), subjectType: v.string(), subjectKey: v.string(), evidenceReference: v.string(), approvedAt: v.number(), expiresAt: v.union(v.number(), v.null()), active: v.boolean() })),
  handler: async (ctx, args) => {
    await requireCatalogue(ctx, args.schoolId, "admissions.catalogue.manage");
    const rows = await ctx.db.query("schoolApprovalEvidence").withIndex("by_school_and_approval_class", (q) => q.eq("schoolId", args.schoolId)).take(200);
    const now = Date.now(); return rows.map((row) => ({ id: row._id, approvalClass: row.approvalClass, subjectType: row.subjectType, subjectKey: row.subjectKey, evidenceReference: row.evidenceReference, approvedAt: row.approvedAt, expiresAt: row.expiresAt ?? null, active: !row.revokedAt && row.approvedAt <= now && (!row.expiresAt || row.expiresAt > now) }));
  },
});

export const listDeclarations = query({
  args: { schoolId: v.id("schools") },
  returns: v.array(v.object({ id: v.id("admissionsDeclarationVersions"), programmeId: v.id("admissionsProgrammes"), version: v.number(), title: v.string(), body: v.string(), purpose: v.string(), status: v.string() })),
  handler: async (ctx, args) => { await requireCatalogue(ctx, args.schoolId, "admissions.catalogue.manage"); const programmes = await ctx.db.query("admissionsProgrammes").withIndex("by_school", (q) => q.eq("schoolId", args.schoolId)).take(100); const rows = (await Promise.all(programmes.map((programme) => ctx.db.query("admissionsDeclarationVersions").withIndex("by_programme_and_status", (q) => q.eq("programmeId", programme._id)).take(100)))).flat(); return rows.map((row) => ({ id: row._id, programmeId: row.programmeId, version: row.version, title: row.title, body: row.body, purpose: row.purpose, status: row.status })); },
});

export const listProductPrices = query({
  args: { productId: v.id("admissionsProducts") },
  returns: v.array(v.object({ id: v.id("admissionsProductPrices"), version: v.number(), amountMinor: v.number(), currency: v.string(), refundPolicyKey: v.string(), feeDisclosure: v.string(), effectiveFrom: v.number(), effectiveTo: v.union(v.number(), v.null()), status: v.string(), approvalEvidenceId: v.union(v.id("schoolApprovalEvidence"), v.null()) })),
  handler: async (ctx, args) => { const product = await ctx.db.get(args.productId); if (!product) throw new ConvexError("Not found or access denied"); await requireCatalogue(ctx, product.schoolId, "admissions.catalogue.manage"); const rows = await ctx.db.query("admissionsProductPrices").withIndex("by_product_and_version", (q) => q.eq("productId", product._id)).order("desc").take(100); return rows.map((row) => ({ id: row._id, version: row.version, amountMinor: row.amountMinor, currency: row.currency, refundPolicyKey: row.refundPolicyKey, feeDisclosure: row.feeDisclosure, effectiveFrom: row.effectiveFrom, effectiveTo: row.effectiveTo ?? null, status: row.status, approvalEvidenceId: row.approvalEvidenceId ?? null })); },
});

export const listConversionClasses = query({
  args: { schoolId: v.id("schools") },
  returns: v.array(v.object({ id: v.id("classes"), name: v.string() })),
  handler: async (ctx, args) => { await requireCatalogue(ctx, args.schoolId, "admissions.catalogue.manage"); const rows = await ctx.db.query("classes").withIndex("by_school", (q) => q.eq("schoolId", args.schoolId)).take(200); return rows.filter((row) => !row.isArchived).map((row) => ({ id: row._id, name: row.name })); },
});

export const createProgramme = mutation({
  args: { schoolId: v.id("schools"), slug: v.string(), name: v.string(), description: v.optional(v.string()) }, returns: v.id("admissionsProgrammes"),
  handler: async (ctx, args) => { const member = await requireCatalogue(ctx, args.schoolId, "admissions.catalogue.manage"); const programmeSlug = slug(args.slug); const existing = await ctx.db.query("admissionsProgrammes").withIndex("by_school_and_slug", (q) => q.eq("schoolId", args.schoolId).eq("slug", programmeSlug)).unique(); if (existing) throw new ConvexError("Programme slug already exists"); const now = Date.now(); const id = await ctx.db.insert("admissionsProgrammes", { schoolId: args.schoolId, slug: programmeSlug, name: text(args.name, "Programme name", 256), ...(args.description?.trim() ? { description: args.description.trim().slice(0, 4_000) } : {}), status: "draft", createdAt: now, updatedAt: now }); await audit({ ctx, schoolId: args.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.programme_created", entityType: "programme", entityId: String(id), outcome: "success" }); return id; },
});

export const createIntake = mutation({
  args: { schoolId: v.id("schools"), programmeId: v.id("admissionsProgrammes"), slug: v.string(), name: v.string(), cycleLabel: v.string(), opensAt: v.number(), closesAt: v.number(), targetClassId: v.optional(v.id("classes")) }, returns: v.id("admissionsIntakes"),
  handler: async (ctx, args) => { const member = await requireCatalogue(ctx, args.schoolId, "admissions.catalogue.manage"); const programme = await ctx.db.get(args.programmeId); const intakeSlug = slug(args.slug); if (!programme || programme.schoolId !== args.schoolId || !Number.isFinite(args.opensAt) || args.opensAt >= args.closesAt) throw new ConvexError("Invalid intake"); if (args.targetClassId) { const classDoc = await ctx.db.get(args.targetClassId); if (!classDoc || classDoc.schoolId !== args.schoolId) throw new ConvexError("Invalid intake"); } const duplicate = await ctx.db.query("admissionsIntakes").withIndex("by_school_and_slug", (q) => q.eq("schoolId", args.schoolId).eq("slug", intakeSlug)).unique(); if (duplicate) throw new ConvexError("Intake slug already exists"); const now = Date.now(); const id = await ctx.db.insert("admissionsIntakes", { schoolId: args.schoolId, programmeId: programme._id, slug: intakeSlug, name: text(args.name, "Intake name", 256), cycleLabel: text(args.cycleLabel, "Cycle label", 128), ...(args.targetClassId ? { targetClassId: args.targetClassId } : {}), opensAt: args.opensAt, closesAt: args.closesAt, status: "draft", createdAt: now, updatedAt: now }); await audit({ ctx, schoolId: args.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.intake_created", entityType: "intake", entityId: String(id), outcome: "success" }); return id; },
});

/** Availability changes are explicit publication actions; they never rewrite existing applications. */
export const setProgrammeStatus = mutation({
  args: { programmeId: v.id("admissionsProgrammes"), status: v.union(v.literal("published"), v.literal("closed")) }, returns: v.null(),
  handler: async (ctx, args) => { const programme = await ctx.db.get(args.programmeId); if (!programme) throw new ConvexError("Not found or access denied"); const member = await requireCatalogue(ctx, programme.schoolId, "admissions.publish"); const now = Date.now(); await ctx.db.patch(programme._id, { status: args.status, updatedAt: now }); await audit({ ctx, schoolId: programme.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.programme_status_changed", entityType: "programme", entityId: String(programme._id), outcome: "success" }); return null; },
});

export const setIntakeStatus = mutation({
  args: { intakeId: v.id("admissionsIntakes"), status: v.union(v.literal("open"), v.literal("paused"), v.literal("closed"), v.literal("archived")) }, returns: v.null(),
  handler: async (ctx, args) => { const intake = await ctx.db.get(args.intakeId); if (!intake) throw new ConvexError("Not found or access denied"); const member = await requireCatalogue(ctx, intake.schoolId, "admissions.publish"); const now = Date.now(); await ctx.db.patch(intake._id, { status: args.status, updatedAt: now }); await audit({ ctx, schoolId: intake.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.intake_status_changed", entityType: "intake", entityId: String(intake._id), outcome: "success" }); return null; },
});

export const setProductStatus = mutation({
  args: { productId: v.id("admissionsProducts"), status: v.union(v.literal("active"), v.literal("paused"), v.literal("retired")) }, returns: v.null(),
  handler: async (ctx, args) => { const product = await ctx.db.get(args.productId); if (!product) throw new ConvexError("Not found or access denied"); const member = await requireCatalogue(ctx, product.schoolId, "admissions.publish"); const now = Date.now(); await ctx.db.patch(product._id, { status: args.status, updatedAt: now }); await audit({ ctx, schoolId: product.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.product_status_changed", entityType: "product", entityId: String(product._id), outcome: "success" }); return null; },
});

export const retireForm = mutation({
  args: { formVersionId: v.id("admissionsFormVersions") }, returns: v.null(),
  handler: async (ctx, args) => { const form = await ctx.db.get(args.formVersionId); if (!form || form.status !== "published") throw new ConvexError("Form is not retireable"); const member = await requireCatalogue(ctx, form.schoolId, "admissions.publish"); const now = Date.now(); await ctx.db.patch(form._id, { status: "retired", updatedAt: now }); await audit({ ctx, schoolId: form.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.form_retired", entityType: "form", entityId: String(form._id), outcome: "success" }); return null; },
});

export const createDraftForm = mutation({
  args: { schoolId: v.id("schools"), programmeId: v.id("admissionsProgrammes"), intakeId: v.id("admissionsIntakes"), schemaVersion: v.string() }, returns: v.id("admissionsFormVersions"),
  handler: async (ctx, args) => {
    const member = await requireCatalogue(ctx, args.schoolId, "admissions.catalogue.manage");
    const [programme, intake, versions]: [Doc<"admissionsProgrammes"> | null, Doc<"admissionsIntakes"> | null, Doc<"admissionsFormVersions">[]] = await Promise.all([
      ctx.db.get("admissionsProgrammes", args.programmeId),
      ctx.db.get("admissionsIntakes", args.intakeId),
      ctx.db.query("admissionsFormVersions").withIndex("by_school_and_programme", (q) => q.eq("schoolId", args.schoolId).eq("programmeId", args.programmeId)).take(100),
    ]);
    if (!programme || !intake || programme.schoolId !== args.schoolId || intake.schoolId !== args.schoolId || intake.programmeId !== programme._id) throw new ConvexError("Invalid form scope");
    const now = Date.now();
    const id = await ctx.db.insert("admissionsFormVersions", { schoolId: args.schoolId, programmeId: programme._id, intakeId: intake._id, version: Math.max(0, ...versions.map((item: Doc<"admissionsFormVersions">) => item.version)) + 1, schemaVersion: text(args.schemaVersion, "Schema version", 128), legalNamePolicyVersion: 2, status: "draft", createdAt: now, updatedAt: now });
    await audit({ ctx, schoolId: args.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.form_drafted", entityType: "form", entityId: String(id), outcome: "success" });
    return id;
  },
});

export const addDraftField = mutation({
  args: { formVersionId: v.id("admissionsFormVersions"), fieldKey: v.string(), sectionKey: v.string(), kind: v.string(), label: v.string(), requiredMode: v.union(v.literal("required"), v.literal("optional"), v.literal("conditional")), dataClass: v.union(v.literal("public"), v.literal("internal"), v.literal("personal"), v.literal("child_confidential"), v.literal("highly_sensitive"), v.literal("financial_security")), purpose: v.optional(v.string()), retentionPolicyKey: v.optional(v.string()), audience: v.optional(v.string()), approvalEvidenceId: v.optional(v.id("schoolApprovalEvidence")), validationJson: v.string(), conditionalRuleJson: v.optional(v.string()), order: v.number() }, returns: v.id("admissionsFormFields"),
  handler: async (ctx, args) => { const form = await ctx.db.get(args.formVersionId); if (!form || form.status !== "draft") throw new ConvexError("Form is not editable"); const member = await requireCatalogue(ctx, form.schoolId, "admissions.catalogue.manage"); if (!(admissionFieldKinds as readonly string[]).includes(args.kind)) throw new ConvexError("Field kind is invalid"); await assertSensitiveConfiguration(ctx, form.schoolId, args.dataClass, args.approvalEvidenceId, args.purpose, args.retentionPolicyKey, args.audience); assertClosedValidationGrammar(args.validationJson); if (args.requiredMode === "conditional" && !args.conditionalRuleJson) throw new ConvexError("Conditional rule is required"); if (args.conditionalRuleJson) assertClosedConditionalGrammar(args.conditionalRuleJson); const key = slug(args.fieldKey); const duplicate = await ctx.db.query("admissionsFormFields").withIndex("by_form_version_and_field_key", (q) => q.eq("formVersionId", form._id).eq("fieldKey", key)).unique(); if (duplicate || !Number.isInteger(args.order) || args.order < 0) throw new ConvexError("Invalid field"); const now = Date.now(); const id = await ctx.db.insert("admissionsFormFields", { schoolId: form.schoolId, formVersionId: form._id, fieldKey: key, sectionKey: text(args.sectionKey, "Section", 128), kind: text(args.kind, "Field kind", 64), label: text(args.label, "Field label", 256), requiredMode: args.requiredMode, dataClass: args.dataClass, ...(args.purpose?.trim() ? { purpose: args.purpose.trim().slice(0, 1_000) } : {}), ...(args.retentionPolicyKey?.trim() ? { retentionPolicyKey: args.retentionPolicyKey.trim().slice(0, 128) } : {}), ...(args.audience?.trim() ? { audience: args.audience.trim().slice(0, 128) } : {}), ...(args.approvalEvidenceId ? { approvalEvidenceId: args.approvalEvidenceId } : {}), validationJson: args.validationJson, ...(args.conditionalRuleJson ? { conditionalRuleJson: args.conditionalRuleJson } : {}), order: args.order, status: "active", createdAt: now, updatedAt: now }); await audit({ ctx, schoolId: form.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.field_added", entityType: "form_field", entityId: String(id), outcome: "success" }); return id; },
});

export const addDraftDocumentRequirement = mutation({
  args: { formVersionId: v.id("admissionsFormVersions"), requirementKey: v.string(), category: v.string(), label: v.string(), requiredMode: v.union(v.literal("required"), v.literal("optional"), v.literal("conditional")), acceptedMimeTypes: v.array(v.string()), maxBytes: v.number(), maxFiles: v.number(), sensitivity: v.union(v.literal("public"), v.literal("internal"), v.literal("personal"), v.literal("child_confidential"), v.literal("highly_sensitive"), v.literal("financial_security")), purpose: v.string(), retentionPolicyKey: v.optional(v.string()), audience: v.optional(v.string()), approvalEvidenceId: v.optional(v.id("schoolApprovalEvidence")), conditionJson: v.optional(v.string()), order: v.number() }, returns: v.id("admissionsDocumentRequirements"),
  handler: async (ctx, args) => { const form = await ctx.db.get(args.formVersionId); if (!form || form.status !== "draft") throw new ConvexError("Form is not editable"); const member = await requireCatalogue(ctx, form.schoolId, "admissions.catalogue.manage"); await assertSensitiveConfiguration(ctx, form.schoolId, args.sensitivity, args.approvalEvidenceId, args.purpose, args.retentionPolicyKey, args.audience); const key = slug(args.requirementKey); if (!Number.isInteger(args.maxBytes) || args.maxBytes < 1 || args.maxBytes > 50_000_000 || !Number.isInteger(args.maxFiles) || args.maxFiles < 1 || args.maxFiles > 10 || args.acceptedMimeTypes.length < 1 || args.acceptedMimeTypes.length > 20 || args.acceptedMimeTypes.some((mime) => !/^[\w.+-]+\/[\w.+-]+$/.test(mime)) || (args.requiredMode === "conditional" && !args.conditionJson)) throw new ConvexError("Invalid document requirement"); if (args.conditionJson) assertClosedConditionalGrammar(args.conditionJson); const duplicate = await ctx.db.query("admissionsDocumentRequirements").withIndex("by_form_version_and_requirement_key", (q) => q.eq("formVersionId", form._id).eq("requirementKey", key)).unique(); if (duplicate) throw new ConvexError("Requirement key already exists"); const now = Date.now(); const id = await ctx.db.insert("admissionsDocumentRequirements", { schoolId: form.schoolId, formVersionId: form._id, requirementKey: key, category: text(args.category, "Document category", 128), label: text(args.label, "Document label", 256), requiredMode: args.requiredMode, acceptedMimeTypes: args.acceptedMimeTypes, maxBytes: args.maxBytes, maxFiles: args.maxFiles, sensitivity: args.sensitivity, purpose: text(args.purpose, "Document purpose", 1_000), ...(args.retentionPolicyKey?.trim() ? { retentionPolicyKey: args.retentionPolicyKey.trim().slice(0, 128) } : {}), ...(args.audience?.trim() ? { audience: args.audience.trim().slice(0, 128) } : {}), ...(args.approvalEvidenceId ? { approvalEvidenceId: args.approvalEvidenceId } : {}), ...(args.conditionJson ? { conditionJson: args.conditionJson } : {}), order: args.order, createdAt: now, updatedAt: now }); await audit({ ctx, schoolId: form.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.document_requirement_added", entityType: "document_requirement", entityId: String(id), outcome: "success" }); return id; },
});

/** Publishing is one-way: draft rows become immutable and an intake can expose exactly one published form. */
export const publishForm = mutation({
  args: { formVersionId: v.id("admissionsFormVersions") }, returns: v.null(),
  handler: async (ctx, args) => { const form = await ctx.db.get(args.formVersionId); if (!form || form.status !== "draft" || !form.intakeId) throw new ConvexError("Form is not publishable"); const member = await requireCatalogue(ctx, form.schoolId, "admissions.publish"); const [fields, requirements] = await Promise.all([ctx.db.query("admissionsFormFields").withIndex("by_form_version_and_order", (q) => q.eq("formVersionId", form._id)).take(200), ctx.db.query("admissionsDocumentRequirements").withIndex("by_form_version_and_order", (q) => q.eq("formVersionId", form._id)).take(100)]); for (const field of fields) { assertClosedValidationGrammar(field.validationJson); if (field.conditionalRuleJson) assertClosedConditionalGrammar(field.conditionalRuleJson); await assertSensitiveConfiguration(ctx, form.schoolId, field.dataClass, field.approvalEvidenceId, field.purpose, field.retentionPolicyKey, field.audience, { type: "admissions_field", key: field.fieldKey }); } for (const requirement of requirements) { if (requirement.conditionJson) assertClosedConditionalGrammar(requirement.conditionJson); await assertSensitiveConfiguration(ctx, form.schoolId, requirement.sensitivity, requirement.approvalEvidenceId, requirement.purpose, requirement.retentionPolicyKey, requirement.audience, { type: "admissions_document_requirement", key: requirement.requirementKey }); } const existing = await ctx.db.query("admissionsFormVersions").withIndex("by_intake_and_status", (q) => q.eq("intakeId", form.intakeId!).eq("status", "published")).take(2); if (existing.length) throw new ConvexError("Retire the current published form before publishing"); const now = Date.now(); await ctx.db.patch(form._id, { status: "published", publishedAt: now, publishedBy: member.userId, updatedAt: now }); await audit({ ctx, schoolId: form.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.form_published", entityType: "form", entityId: String(form._id), outcome: "success" }); return null; },
});

/** Declaration text is drafted first; only a separate publish operation can make it guardian-visible. */
export const createDeclaration = mutation({
  args: { schoolId: v.id("schools"), programmeId: v.id("admissionsProgrammes"), version: v.number(), title: v.string(), body: v.string(), purpose: v.string() }, returns: v.id("admissionsDeclarationVersions"),
  handler: async (ctx, args) => { const member = await requireCatalogue(ctx, args.schoolId, "admissions.catalogue.manage"); const programme = await ctx.db.get(args.programmeId); if (!programme || programme.schoolId !== args.schoolId || !Number.isInteger(args.version) || args.version < 1) throw new ConvexError("Invalid declaration"); const existing = await ctx.db.query("admissionsDeclarationVersions").withIndex("by_school_and_programme_and_version", (q) => q.eq("schoolId", args.schoolId).eq("programmeId", programme._id).eq("version", args.version)).unique(); if (existing) throw new ConvexError("Declaration version exists"); const now = Date.now(); const body = text(args.body, "Declaration body", 20_000); const id = await ctx.db.insert("admissionsDeclarationVersions", { schoolId: args.schoolId, programmeId: programme._id, version: args.version, title: text(args.title, "Declaration title", 256), body, bodyDigest: await digest(body), purpose: text(args.purpose, "Declaration purpose", 1_000), status: "draft", createdAt: now, updatedAt: now }); await audit({ ctx, schoolId: args.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.declaration_drafted", entityType: "declaration", entityId: String(id), outcome: "success" }); return id; },
});

export const publishDeclaration = mutation({
  args: { declarationVersionId: v.id("admissionsDeclarationVersions") }, returns: v.null(),
  handler: async (ctx, args) => { const declaration = await ctx.db.get(args.declarationVersionId); if (!declaration || declaration.status !== "draft") throw new ConvexError("Declaration is not publishable"); const member = await requireCatalogue(ctx, declaration.schoolId, "admissions.publish"); const active = await ctx.db.query("admissionsDeclarationVersions").withIndex("by_programme_and_status", (q) => q.eq("programmeId", declaration.programmeId).eq("status", "published")).take(2); const now = Date.now(); for (const prior of active) await ctx.db.patch(prior._id, { status: "retired", updatedAt: now }); await ctx.db.patch(declaration._id, { status: "published", publishedAt: now, publishedBy: member.userId, updatedAt: now }); await audit({ ctx, schoolId: declaration.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.declaration_published", entityType: "declaration", entityId: String(declaration._id), outcome: "success" }); return null; },
});

export const retireDeclaration = mutation({
  args: { declarationVersionId: v.id("admissionsDeclarationVersions") }, returns: v.null(),
  handler: async (ctx, args) => { const declaration = await ctx.db.get(args.declarationVersionId); if (!declaration || declaration.status !== "published") throw new ConvexError("Declaration is not retireable"); const member = await requireCatalogue(ctx, declaration.schoolId, "admissions.publish"); await ctx.db.patch(declaration._id, { status: "retired", updatedAt: Date.now() }); await audit({ ctx, schoolId: declaration.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.declaration_retired", entityType: "declaration", entityId: String(declaration._id), outcome: "success" }); return null; },
});


export const createProduct = mutation({
  args: { schoolId: v.id("schools"), intakeId: v.id("admissionsIntakes"), slug: v.string(), name: v.string() }, returns: v.id("admissionsProducts"),
  handler: async (ctx, args) => { const member = await requireCatalogue(ctx, args.schoolId, "admissions.catalogue.manage"); const intake = await ctx.db.get(args.intakeId); const productSlug = slug(args.slug); if (!intake || intake.schoolId !== args.schoolId) throw new ConvexError("Invalid product"); const duplicate = await ctx.db.query("admissionsProducts").withIndex("by_school_and_slug", (q) => q.eq("schoolId", args.schoolId).eq("slug", productSlug)).unique(); if (duplicate) throw new ConvexError("Product slug already exists"); const now = Date.now(); const id = await ctx.db.insert("admissionsProducts", { schoolId: args.schoolId, intakeId: intake._id, slug: productSlug, name: text(args.name, "Product name", 256), slotCount: 1, status: "draft", createdAt: now, updatedAt: now }); await audit({ ctx, schoolId: args.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.product_created", entityType: "product", entityId: String(id), outcome: "success" }); return id; },
});

export const publishPrice = mutation({
  args: { productId: v.id("admissionsProducts"), version: v.number(), amountMinor: v.number(), currency: v.string(), refundPolicyKey: v.string(), feeDisclosure: v.string(), effectiveFrom: v.number(), effectiveTo: v.optional(v.number()), approvalEvidenceId: v.id("schoolApprovalEvidence") }, returns: v.id("admissionsProductPrices"),
  handler: async (ctx, args) => { const product = await ctx.db.get(args.productId); if (!product || !Number.isInteger(args.version) || args.version < 1 || !Number.isInteger(args.amountMinor) || args.amountMinor < 0 || !Number.isFinite(args.effectiveFrom) || (args.effectiveTo !== undefined && args.effectiveTo <= args.effectiveFrom)) throw new ConvexError("Invalid price"); const member = await requireCatalogue(ctx, product.schoolId, "admissions.publish"); const evidence = await ctx.db.get(args.approvalEvidenceId); const approver = evidence?.approvedByUserId ? await ctx.db.get(evidence.approvedByUserId) : null; if (!evidence || evidence.schoolId !== product.schoolId || evidence.approvalClass !== "finance" || evidence.subjectType !== "admissions_price" || evidence.subjectKey !== `${String(product._id)}:${args.version}` || evidence.revokedAt || evidence.approvedAt > Date.now() || (evidence.expiresAt && evidence.expiresAt <= Date.now()) || !approver || approver.schoolId !== product.schoolId || approver.isArchived) throw new ConvexError("Finance approval is unavailable"); const existing = await ctx.db.query("admissionsProductPrices").withIndex("by_product_and_version", (q) => q.eq("productId", product._id).eq("version", args.version)).unique(); if (existing) throw new ConvexError("Price version exists"); const now = Date.now(); const id = await ctx.db.insert("admissionsProductPrices", { schoolId: product.schoolId, productId: product._id, version: args.version, amountMinor: args.amountMinor, currency: text(args.currency, "Currency", 8).toUpperCase(), refundPolicyKey: text(args.refundPolicyKey, "Refund policy", 128), feeDisclosure: text(args.feeDisclosure, "Fee disclosure", 4_000), effectiveFrom: args.effectiveFrom, ...(args.effectiveTo !== undefined ? { effectiveTo: args.effectiveTo } : {}), status: "published", approvalEvidenceId: args.approvalEvidenceId, createdAt: now, updatedAt: now }); await audit({ ctx, schoolId: product.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.price_published", entityType: "product_price", entityId: String(id), outcome: "success" }); return id; },
});

export const updateIntakeDetails = mutation({
  args: {
    schoolId: v.id("schools"),
    intakeId: v.id("admissionsIntakes"),
    name: v.string(),
    opensAt: v.number(),
    closesAt: v.number(),
    cycleLabel: v.string(),
    description: v.optional(v.string()),
  },
  returns: v.null(),
  handler: async (ctx, args) => {
    const member = await requireCatalogue(ctx, args.schoolId, "admissions.catalogue.manage");
    const intake = await ctx.db.get(args.intakeId);
    if (!intake || intake.schoolId !== args.schoolId || !Number.isFinite(args.opensAt) || args.opensAt >= args.closesAt) throw new ConvexError("Invalid intake");
    const now = Date.now();
    const name = text(args.name, "Intake name", 256);
    await ctx.db.patch(intake._id, {
      name,
      opensAt: args.opensAt,
      closesAt: args.closesAt,
      cycleLabel: text(args.cycleLabel, "Cycle label", 128),
      updatedAt: now,
    });
    const programmeIntakes = await ctx.db.query("admissionsIntakes").withIndex("by_programme_and_status", (q) => q.eq("programmeId", intake.programmeId)).take(2);
    if (programmeIntakes.length === 1) {
      await ctx.db.patch(intake.programmeId, {
        name,
        ...(args.description !== undefined ? { description: args.description.trim().slice(0, 4_000) } : {}),
        updatedAt: now,
      });
    }
    await audit({ ctx, schoolId: args.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.intake_updated", entityType: "intake", entityId: String(intake._id), outcome: "success" });
    return null;
  },
});

/** Only unused draft campaigns can be deleted; all related rows are bounded and loaded before mutation. */
export const deleteIntake = mutation({
  args: { schoolId: v.id("schools"), intakeId: v.id("admissionsIntakes") },
  returns: v.null(),
  handler: async (ctx, args) => {
    const member = await requireCatalogue(ctx, args.schoolId, "admissions.catalogue.manage");
    const intake = await ctx.db.get(args.intakeId);
    if (!intake || intake.schoolId !== args.schoolId || intake.status !== "draft") throw new ConvexError("Only unused draft campaigns can be deleted");
    const [applications, programmeIntakes, formVersions, declarations, products] = await Promise.all([
      ctx.db.query("admissionsApplications").withIndex("by_school_and_intake_and_state", (q) => q.eq("schoolId", args.schoolId).eq("intakeId", intake._id)).take(1),
      ctx.db.query("admissionsIntakes").withIndex("by_programme_and_status", (q) => q.eq("programmeId", intake.programmeId)).take(2),
      ctx.db.query("admissionsFormVersions").withIndex("by_intake_and_status", (q) => q.eq("intakeId", intake._id)).take(101),
      ctx.db.query("admissionsDeclarationVersions").withIndex("by_programme_and_status", (q) => q.eq("programmeId", intake.programmeId)).take(101),
      ctx.db.query("admissionsProducts").withIndex("by_school_and_intake", (q) => q.eq("schoolId", args.schoolId).eq("intakeId", intake._id)).take(21),
    ]);
    if (applications.length || programmeIntakes.length !== 1 || formVersions.length > 100 || declarations.length > 100 || products.length > 20) throw new ConvexError("Draft campaign cannot be deleted safely");
    const formChildren = await Promise.all(formVersions.map(async (form) => {
      const [fields, requirements] = await Promise.all([
        ctx.db.query("admissionsFormFields").withIndex("by_form_version_and_order", (q) => q.eq("formVersionId", form._id)).take(201),
        ctx.db.query("admissionsDocumentRequirements").withIndex("by_form_version_and_order", (q) => q.eq("formVersionId", form._id)).take(101),
      ]);
      if (fields.length > 200 || requirements.length > 100) throw new ConvexError("Draft campaign cannot be deleted safely");
      return { form, fields, requirements };
    }));
    const productPrices = await Promise.all(products.map(async (product) => {
      const prices = await ctx.db.query("admissionsProductPrices").withIndex("by_product_and_version", (q) => q.eq("productId", product._id)).take(101);
      if (prices.length > 100) throw new ConvexError("Draft campaign cannot be deleted safely");
      return { product, prices };
    }));
    for (const { form, fields, requirements } of formChildren) {
      for (const field of fields) await ctx.db.delete(field._id);
      for (const requirement of requirements) await ctx.db.delete(requirement._id);
      await ctx.db.delete(form._id);
    }
    for (const declaration of declarations) await ctx.db.delete(declaration._id);
    for (const { product, prices } of productPrices) {
      for (const price of prices) await ctx.db.delete(price._id);
      await ctx.db.delete(product._id);
    }
    await ctx.db.delete(intake._id);
    await ctx.db.delete(intake.programmeId);
    await audit({ ctx, schoolId: args.schoolId, actor: { kind: "staff", userId: member.userId }, action: "catalogue.intake_deleted", entityType: "intake", entityId: String(intake._id), outcome: "success" });
    return null;
  },
});
