# S0 commit boundaries and PR split

This is an assignment map for manual review, not a staging script. **Do not use speculative automatic staging.** Every shared file below requires a human hunk split (or a serialized owner commit); do not stage a whole file merely because one slice owns part of it. Preserve unrelated staged work and preserve the independent architecture-doc commit.

## Dependency-aware boundaries

| Commit / PR boundary | Depends on | Primary files and hunks to assign | Boundary notes |
|---|---|---|---|
| **U1 — governance and workspace access** | G0 | `packages/convex/functions/auth.ts`; `academic/auth.ts`, `academic/rbac.ts`, `academic/groups.ts`, `academic/audit.ts`, `academic/groupSettings.ts`, `academic/groupOverview.ts`; `packages/shared/src/workspace-access.ts`, `workspace-navigation.ts`, `workspace-route-access.ts`, `capability-contract.ts`, `workspace-capability-matrix.ts`, `group-settings.ts`, `audit-export.ts`; `components/WorkspaceNavbar.tsx`, `BranchSwitcher.tsx`, `AuthoritativeForbiddenView.tsx`; Admin/Teacher `AuthProvider.tsx`, `StaffWorkspace.tsx`, route layouts; Platform `/groups`, `/audit`; Admin `/admin/group`, `/admin/permissions`, `/admin/audit` and their errors/components. | Sequence U1a → U1b → U1c → U1d → U1e → U1f → U1g. U1 owns only governance/access/audit/default/overview hunks. Route callers belonging to U2–U6 stay with their domain commits and are manually split from layouts/navigation. R1 managed-capability/security remediation hunks stay with U1 security owner, not silently attributed to each domain. |
| **U2 — academic and finance configuration** | U1a/U1b; U1f contracts where consumed | `academic/gradingBands.ts`, `admissionNumbers.ts`, `studentEnrollment.ts`, `bankAccounts.ts`; `billing.ts`, `billingShared.ts`; grading-band Admin routes/components and `apps/admin/lib/types.ts`; admission-numbering Admin route; billing bank components/pages, `SettingsPanel.tsx`, `PrintableFinanceModal.tsx`, billing forms/types; shared `exam-recording/*`, `ReportCardSheet.tsx`, invoice-instruction component/tests; Admin/Teacher/Portal grade-consumer hunks and `reportCards.ts`/`portal.ts` payload hunks; related focused tests. | Review as ordered subcommits: U2a → U2b → U2c → U2d, with U2d after U1e audit seam. U2b consumer hunks must not absorb U3d theme-only changes. U2c numbering handoff precedes U3b and U6a acceptance. U2d billing handoff precedes U3c/U5a. |
| **U3 — drafts, forms, and theme** | U1a; U1b guard handshake; U2a/U2c/U2d as applicable | `packages/shared/src/drafts/**`; `academic/drafts.ts`; `UnsavedBranchSwitchModal.tsx`, `MobileProgressIndicator.tsx`; Admin/Teacher draft connection and form pages/components/tests (`academic/students/onboarding/**`, family/teacher forms, billing forms, session/term, exam-recording, report-card-bundles); Teacher planning/lesson-plan files; `packages/shared/src/theme/**`; `academic/schoolBranding.ts`; Admin settings/theme; Admin/Teacher/Portal/Sites Tailwind and Sites `lib/site.ts`, `lib/site-ui.tsx`, package manifest; theme audit/AGENTS documentation hunks. | Ordered subcommits: U3a core/lifecycle → U3b people → U3c long forms → U3d theme. U3a must not claim form adoption. Keep U4b migration adoption in U4. Theme hunk ownership is separate from U1b navbar hunk and U2b print/grade hunk; manually split `ReportCardSheet.tsx`, navbar, layouts, shared index and package manifests. |
| **U4 — institutional email and reviewed import** | U1d/U3a/U3b/U3c; U2c numbering | `academic/institutionalEmail.ts`; Admin email-domain route and student/teacher email-link hunks; `academic/aiImport.ts`, `migrationWorkspace.ts`, `migrationIngest.ts`, `migrationAutosave.ts`, `migrationMerge.ts`; shared migration components/parsers; Admin import layout/workbench and Platform school migration wrapper; email/import/migration tests. | U4a precedes U4b. Import commit remains a release-blocking code boundary: the unsafe public commit must be disabled or fully replaced before this PR is releasable. Do not mix migration fixes with U3c planning or U2c allocator hunks except explicit adapter interfaces. |
| **U5 — commercial, usage, assets, lifecycle** | U1c/U1d/U1e; U2d; U3c; U5c after U5b for entitlement binding | `academic/commercial.ts`, `metering.ts`, `assets.ts`, `assetWorkspace.ts`, `assetStorageBoundary.ts`; `foundation/commercialContract.ts`, `paidUsageGate.ts`; additive commercial/usage/assets schema hunks; Platform commercial/usage components/routes; Admin billing subscription/settlements/usage and assets/** routes/components; Teacher heavy-operation gating/library hunks; asset/commercial/usage tests. | Ordered subcommits: U5a → U5b → U5c → U5d. Keep commercial catalog/contract separate from asset library. U5b entitlement/reservation contract must precede U5c upload transport; U5c owns library intake and U5d lifecycle/PDF. No provider activation, payment, AV, or destructive cleanup. |
| **U6 — within-group transfers** | U1c/U1d/U1e; U2c; U3a | `academic/transfers.ts`; `studentTransfers` schema hunks; Admin transfer route/error, student list/profile/history links; transfer integration/UI tests; only corresponding Academic navigation hunk. | Keep allocator implementation in U2c and consume its helper; do not fork numbering. Transfer Portal identity continuity is a required follow-up code hunk, not an external/runtime-only checkbox. |
| **HEAD architecture docs (independent; preserve)** | Existing HEAD history | Preserve the separately authored architecture documentation commit included after base `f6fc7c4817eb287daeebf78a4d143ef1a988844f`, specifically the architecture-doc change such as `docs/features/ProductWideModuleEntitlements.md` as recorded by the stabilization checkpoint. | Do not squash, reattribute, or fold this commit into U1–U6. Check its commit identity against checkpoint/HEAD before any future history operation. |
| **S0/final artifacts (separate, last)** | U1–U6 inventory only | `docs/tasks/orchestrator-sessions/orch-20260905-032529/results/S0-packet-status.md`; this file; later U7 report/evidence files only when actually created. | Documentation/review artifacts are not product commits and must not be mixed into implementation PRs. This worker did not stage or commit them. |

## Shared multi-slice files requiring manual hunk split

These are known collision points. Assign individual hunks by the owning boundary, then review the resulting diff; no automatic `git add` pattern is safe:

- `packages/convex/schema.ts`: U1 governance/audit/group fields; U2 grade/number/bank/report fields; U3 draft/theme fields; U4 email/import fields; U5 commercial/usage/assets fields; U6 transfer fields. Generated files are never hand-edited.
- `packages/convex/functions/academic/rbac.ts`: U1a contract/security and U1d management sections; asset/report/domain capability additions must remain in their owning reviewed hunks.
- `packages/convex/functions/academic/auth.ts`, root `functions/auth.ts`: U1a plus later security remediation only; no domain caller changes here.
- `packages/shared/src/index.ts`, package manifests, `pnpm-lock.yaml`: serialize exports/dependency changes; split by exact export/manifest hunk and review lockfile ownership.
- `packages/shared/src/workspace-navigation.ts`, `WorkspaceNavbar.tsx`, `BranchSwitcher.tsx`, `AuthoritativeForbiddenView.tsx`: U1b shell seam; U2–U6 add only explicit route entries/caller adapters in separate hunks.
- Admin/Teacher/Portal route `layout.tsx` files and `apps/admin/app/admin/page.tsx`: U1b shell/access versus U2/U3/U4/U5/U6 links; split by route entry and guard/provider boundary.
- `academic/assets.ts`: U5c upload/library/quarantine versus U5d archive/trash/holds/PDF; never stage whole-file changes without review.
- `academic/groups.ts`, `academic/audit.ts`, `academic/commercial.ts`, `academic/billing.ts`: separate packet sections and security fixes by exact function/hunk.
- `ReportCardSheet.tsx`, report payload files, billing shared renderers: U2b grade/history versus U2d finance instructions versus U3d theme/print; manually split.
- Existing combined integration tests (`commercialAndAssets`, `emailAndAiImport`, migration, security fixtures) and shared/admin shell tests: split test hunks with their implementation owner; preserve unrelated baseline assertions.
- `AGENTS.md` and theme-audit script: documentation/configuration-only changes belong in a reviewed artifact/config commit, not silently with a product slice unless the exact requirement hunk is intentionally assigned to U3d.

## Current index/worktree discrepancy (observed, no mutation)

`git status --short --branch` showed branch `feat/melo-expansion-productization` and a mixed index/worktree, not a cleanly reconciled state:

- Task-recorded baseline reconciliation: **319 changed paths**; status totals **M=142, A=159, AD=13, AM=3, MM=2, untracked=2**.
- Current status includes many staged `M`/`A` entries (the large U1–U6 implementation/test set and earlier docs), while `AD` includes staged `results/R1-api-adoption.json` with the worktree copy deleted; `AM`/`MM` entries include staged content plus further unstaged edits (notably `academicSetup.ts`, `adminLeadership.ts`, `auth.ts`, `institutionalEmail.ts`, `rbac.ts`, `reportCardExtras.ts`, `reportCards.ts`, `billing.ts`, shared capability/matrix files and security tests/fixtures).
- Current untracked paths explicitly observed: `03-stabilization.task.md`, `capability-route-api-matrix.md`, `results/R1-security.md`, `results/S0-auth-manual-review.md`, `results/S0-auth-remediation.md`, and `packages/convex/functions/academic/__tests__/authorizationRemediation.integration.test.ts`. The two documentation artifacts created by this task are also expected to remain untracked until their owner reviews them.
- The status output also showed staged architecture/session documentation and many staged product files; this does **not** prove they belong to one coherent commit. The independent HEAD architecture-doc commit must remain distinct.

No index, code, stash, reset, deletion, restaging, provider, live command, deployment, migration, or credential operation was performed. Before commit creation, reconcile against checkpoint artifacts and inspect each staged/unstaged hunk; do not assume status letters represent packet ownership.
